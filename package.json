{
  "name": "issrc-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:worker": "npm run build && wrangler dev",
    "deploy": "wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "wrangler": "4.103.0"
  }
}
